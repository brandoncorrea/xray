{
  "name": "xray",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "xray": "./src/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "mutate": "mutagen",
    "mutate:all": "mutagen --all --parallel 4 --json reports/mutation/all.json",
    "mutate:all:json": "mutagen --all --parallel 4 --json",
    "mutate:incremental": "mutagen --incremental --json",
    "mutate:diff": "mutagen --diff",
    "test:coverage": "vitest run --coverage",
    "test:coverage:open": "vitest run --coverage && open coverage/index.html"
  },
  "dependencies": {
    "acorn": "^8.16.0",
    "acorn-jsx": "^5.3.2",
    "madge": "^8.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.4",
    "mutagen": "github:brandoncorrea/mutagen",
    "vitest": "^4.1.4"
  }
}
